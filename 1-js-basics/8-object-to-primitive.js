//Object to primitive conversion
// JS doesn’t allow you to customize how operators work on objects.
// E.g. we can’t make objects representing vectors or matrices (or achievements or whatever), add them and expect a “summed” object as the result.


// Conversion rules


